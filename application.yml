spring:
  application:
    name: CONFIG-DEFAULT
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Sunilkumar-Malge/microservice-config-server
          clone-on-start: true
